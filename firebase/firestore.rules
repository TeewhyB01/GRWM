rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all access until collections, ownership, and admin roles are modeled.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
