{
  "name": "@grwm/firebase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"tests/**/*.ts\"",
    "test:firestore-rules": "node --test tests/firestore.rules.test.ts",
    "test:storage-rules": "node --test tests/storage.rules.test.ts",
    "test:firebase-rules": "node --test --test-concurrency=1 tests/*.rules.test.ts",
    "seed:emulator": "node tests/seedLocalEmulator.ts"
  },
  "dependencies": {
    "@grwm/shared": "workspace:*",
    "firebase": "^12.14.0",
    "firebase-admin": "^13.10.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0"
  }
}
